import React from "react";
import Section from "../Layout/Section";

const Hero = () => {
  return (
    <Section>
      <div className={classes.row}></div>
    </Section>
  );
};

export default Hero;
