import React from "react";
import classes from "./Registering.module.scss";

const Registering = () => {
  return (
    <div className={classes.RegisterOptions}>
      <ul className={classes.list}>
        <li className={classes.items}></li>
        <li className={classes.items}></li>
        <li className={classes.items}></li>
      </ul>
    </div>
  );
};

export default Registering;
